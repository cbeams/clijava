apply plugin: 'java'

sourceCompatibility = 1.8

repositories {
    jcenter()
}

dependencies {
    compile 'org.slf4j:slf4j-api:1.7.5'
    compile 'io.beams.libjava:libjava:0.0.2'
    testCompile 'junit:junit:4.11'
}
